# Everything needed to work on the project locally: the API, the Streamlit
# demo, and the test suite. The deployed API installs requirements-api.txt
# instead, which is this list minus the demo and dev tooling.
-r requirements-api.txt

# Streamlit demo (app.py).
plotly>=5.24
streamlit>=1.39

# Test suite.
pytest>=8.3
# Required by fastapi.testclient, which drives the API tests.
httpx>=0.27

# Runtime dependencies for the API and engine. This is what the Docker image
# and Render install, so test and lint tooling lives in requirements-dev.txt.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pandas>=2.2,<3
numpy>=2.0,<3
yfinance>=0.2,<2
